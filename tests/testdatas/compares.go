package testdatas

import "github.com/CSKU-Lab/go-grader/models"

var Compares = []models.CompareConfig{
	{
		ID:          "default",
		BuildScript: "#!/bin/bash\n\ng++ default.cpp -o default",
		RunScript:   "#!/bin/bash\n\n$RUN_NAME input sol_output ./feedback_dir < output",
		RunName:     "default",
		Files: []models.File{
			{
				Name:    "default.cpp",
				Content: "// default_validator from kattis problemtools package\n// licensed under MIT license\n//\n// modified: float comparison and file output location\n#include <fstream>\n#include <iostream>\n#include <string>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <cassert>\n#include <cmath>\n#include <cstdarg>\n#include <sstream>\n#include <vector>\n\nconst int EXIT_AC = 42;\nconst int EXIT_WA = 43;\n\nstd::ifstream judgein, judgeans;\nFILE *judgemessage = NULL;\nFILE *diffpos = NULL;\nint judgeans_pos, stdin_pos;\nint judgeans_line, stdin_line;\n\n/* The floating point type we use internally: */\ntypedef long double flt;\n\nvoid wrong_answer(const char *err, ...) {\n\tva_list pvar;\n\tva_start(pvar, err);\n\tfprintf(judgemessage, \"Wrong answer on line %d of output (corresponding to line %d in answer file)\\n\",\n\t\t\tstdin_line, judgeans_line);\n\tvfprintf(judgemessage, err, pvar);\n\tfprintf(judgemessage, \"\\n\");\n\tif (diffpos) {\n\t\tfprintf(diffpos, \"%d %d\", judgeans_pos, stdin_pos);\n\t}\n\texit(EXIT_WA);\n}\n\nvoid judge_error(const char *err, ...) {\n\tva_list pvar;\n\tva_start(pvar, err);\n\t// If judgemessage hasn't been set up yet, write error to stderr\n\tif (!judgemessage) judgemessage = stderr;\n\tvfprintf(judgemessage, err, pvar);\n\tfprintf(judgemessage, \"\\n\");\n\tassert(!\"Judge Error\");\n}\n\nbool isfloat(const char *s, flt &val) {\n\tchar trash[20];\n\tflt v;\n\tif (sscanf(s, \"%Lf%10s\", &v, trash) != 1) return false;\n\tval = v;\n\treturn true;\n}\n\ntemplate <typename Stream>\nvoid openfile(Stream &stream, const char *file, const char *whoami) {\n\tstream.open(file);\n\tif (stream.fail()) {\n\t\tjudge_error(\"%s: failed to open %s\\n\", whoami, file);\n\t}\n}\n\nFILE *openfeedback(const char *file, const char *whoami) {\n\tFILE *res = fopen(file, \"w\");\n\tif (!res) {\n\t\tjudge_error(\"%s: failed to open %s for writing\", whoami, file);\n\t}\n\treturn res;\n}\n\n/* Test two numbers for equality, accounting for +/-INF, NaN and\n * precision. Float f2 is considered the reference value for relative\n * error.\n */\nint equal(flt f1, flt f2, flt float_abs_tol, flt float_rel_tol)\n{\n\tflt absdiff, reldiff;\n\t/* Finite values are compared with some tolerance */\n\tif ( std::isfinite(f1) && std::isfinite(f2) ) {\n\t\tabsdiff = fabsl(f1-f2);\n\t\treldiff = fabsl((f1-f2)/f2);\n\t\treturn !(absdiff > float_abs_tol && reldiff > float_rel_tol);\n\t}\n\t/* NaN is equal to NaN */\n\tif ( std::isnan(f1) && std::isnan(f2) ) return 1;\n\t/* Infinite values are equal if their sign matches */\n\tif ( std::isinf(f1) && std::isinf(f2) ) {\n\t\treturn std::signbit(f1) == std::signbit(f2);\n\t}\n\t/* Values in different classes are always different. */\n\treturn 0;\n}\n\n// Function to trim leading and trailing whitespace\nstd::string trim(const std::string& str) {\n    size_t first = str.find_first_not_of(\" \\t\\n\\r\");\n    if (first == std::string::npos) return \"\";\n    size_t last = str.find_last_not_of(\" \\t\\n\\r\");\n    return str.substr(first, last - first + 1);\n}\n\n// Function to trim only trailing whitespace\nstd::string trim_trailing(const std::string& str) {\n    size_t first = str.find_first_not_of(\" \\t\\n\\r\");\n    if (first == std::string::npos) return \"\";\n    size_t last = str.find_last_not_of(\" \\t\\n\\r\");\n    return str.substr(0, last + 1);\n}\n\n// Custom streams to handle trimming of trailing whitespace\nclass TrimmingStream {\nprivate:\n    std::istream& stream;\n    std::stringstream line_buffer;\n    bool buffer_empty;\n\npublic:\n    TrimmingStream(std::istream& s) : stream(s), buffer_empty(true) {}\n\n    bool getline(std::string& line) {\n        if (buffer_empty) {\n            std::string raw_line;\n            if (!std::getline(stream, raw_line)) {\n                return false;\n            }\n            line = trim_trailing(raw_line);\n            return true;\n        } else {\n            line = line_buffer.str();\n            buffer_empty = true;\n            return true;\n        }\n    }\n\n    void pushback(const std::string& line) {\n        line_buffer.str(line);\n        buffer_empty = false;\n    }\n\n    int peek() {\n        return stream.peek();\n    }\n\n    int get() {\n        return stream.get();\n    }\n};\n\n// Function to copy file content from one file to another\nvoid copyFileContent(const char* source, const char* destination) {\n    FILE* src = fopen(source, \"r\");\n    if (!src) {\n        std::cerr << \"Failed to open source file: \" << source << std::endl;\n        return;\n    }\n    \n    FILE* dest = fopen(destination, \"w\");\n    if (!dest) {\n        std::cerr << \"Failed to open destination file: \" << destination << std::endl;\n        fclose(src);\n        return;\n    }\n    \n    char buffer[1024];\n    size_t bytesRead;\n    \n    while ((bytesRead = fread(buffer, 1, sizeof(buffer), src)) > 0) {\n        fwrite(buffer, 1, bytesRead, dest);\n    }\n    \n    fclose(src);\n    fclose(dest);\n}\n\nconst char *USAGE = \"Usage: %s judge_in judge_ans feedback_dir [options] < team_out\";\n\nint main(int argc, char **argv) {\n\tif(argc < 4) {\n\t\tjudge_error(USAGE, argv[0]);\n\t}\n\t\n\t// Open judgemessage.txt in the current directory\n\tjudgemessage = openfeedback(\"judgemessage.txt\", argv[0]);\n\t\n\t// Keep the original feedback directory path for diffposition.txt\n\tstd::string feedbackDir = argv[3];\n\tstd::string diffPosPath = feedbackDir + \"/diffposition.txt\";\n\tdiffpos = fopen(diffPosPath.c_str(), \"w\");\n\tif (!diffpos) {\n\t\tjudge_error(\"%s: failed to open %s for writing\", argv[0], diffPosPath.c_str());\n\t}\n\t\n\topenfile(judgein, argv[1], argv[0]);\n\topenfile(judgeans, argv[2], argv[0]);\n\n    // We need to process both judge_ans and team_out line by line, \n    // trimming trailing whitespace before comparison\n    std::string judge_line, team_line;\n    std::vector<std::string> judge_lines, team_lines;\n    \n    // Read and process judge answer file\n    while (std::getline(judgeans, judge_line)) {\n        judge_lines.push_back(trim_trailing(judge_line));\n    }\n    \n    // Read and process team output\n    while (std::getline(std::cin, team_line)) {\n        team_lines.push_back(trim_trailing(team_line));\n    }\n    \n    // Create new stringstreams with the trimmed content\n    std::stringstream trimmed_judgeans, trimmed_stdin;\n    for (const auto& line : judge_lines) {\n        trimmed_judgeans << line << '\\n';\n    }\n    \n    for (const auto& line : team_lines) {\n        trimmed_stdin << line << '\\n';\n    }\n    \n    // Clear and replace the original streams\n    judgeans.clear();\n    judgeans.seekg(0);\n    std::cin.clear();\n    \n    // Create buffer streams that will be used instead of the originals\n    std::istringstream new_judgeans(trimmed_judgeans.str());\n    std::istringstream new_stdin(trimmed_stdin.str());\n\n\tbool case_sensitive = false;\n\tbool space_change_sensitive = false;\n\tbool use_floats = false;\n\tflt float_abs_tol = -1;\n\tflt float_rel_tol = -1;\n\n\tfor (int a = 4; a < argc; ++a) {\n\t\tif        (!strcmp(argv[a], \"case_sensitive\")) {\n\t\t\tcase_sensitive = true;\n\t\t} else if (!strcmp(argv[a], \"space_change_sensitive\")) {\n\t\t\tspace_change_sensitive = true;\n\t\t} else if (!strcmp(argv[a], \"float_absolute_tolerance\")) {\n\t\t\tif (a+1 == argc || !isfloat(argv[a+1], float_abs_tol))\n\t\t\t\tjudge_error(USAGE, argv[0]);\n\t\t\t++a;\n\t\t} else if (!strcmp(argv[a], \"float_relative_tolerance\")) {\n\t\t\tif (a+1 == argc || !isfloat(argv[a+1], float_rel_tol))\n\t\t\t\tjudge_error(USAGE, argv[0]);\n\t\t\t++a;\n\t\t} else if (!strcmp(argv[a], \"float_tolerance\")) {\n\t\t\tif (a+1 == argc || !isfloat(argv[a+1], float_rel_tol))\n\t\t\t\tjudge_error(USAGE, argv[0]);\n\t\t\tfloat_abs_tol = float_rel_tol;\n\t\t\t++a;\n\t\t} else {\n\t\t\tjudge_error(USAGE, argv[0]);\n\t\t}\n\t}\n\tuse_floats = float_abs_tol >= 0 || float_rel_tol >= 0;\n\n\tjudgeans_pos = stdin_pos;\n\tjudgeans_line = stdin_line = 1;\n\n\tstd::string judge, team;\n\twhile (true) {\n\t\t// Space!  Can't live with it, can't live without it...\n\t\twhile (isspace(new_judgeans.peek())) {\n\t\t\tchar c = (char)new_judgeans.get();\n\t\t\tif (space_change_sensitive) {\n\t\t\t\tint d = new_stdin.get();\n\t\t\t\tif (c != d) {\n\t\t\t\t\twrong_answer(\"Space change error: got %d expected %d\", d, c);\n\t\t\t\t}\n\t\t\t\tif (d == '\\n') ++stdin_line;\n\t\t\t\t++stdin_pos;\n\t\t\t}\n\t\t\tif (c == '\\n') ++judgeans_line;\n\t\t\t++judgeans_pos;\n\t\t}\n\t\twhile (isspace(new_stdin.peek())) {\n\t\t\tchar d = (char)new_stdin.get();\n\t\t\tif (space_change_sensitive) {\n\t\t\t\twrong_answer(\"Space change error: judge out of space, got %d from team\", d);\n\t\t\t}\n\t\t\tif (d == '\\n') ++stdin_line;\n\t\t\t++stdin_pos;\n\t\t}\n\n\t\tif (!(new_judgeans >> judge))\n\t\t\tbreak;\n\n\t\tif (!(new_stdin >> team)) {\n\t\t\twrong_answer(\"User EOF while judge had more output\\n(Next judge token: %s)\", judge.c_str());\n\t\t}\n\n\t\tflt jval, tval;\n\t\tif (use_floats && isfloat(judge.c_str(), jval)) {\n\t\t\tif (!isfloat(team.c_str(), tval)) {\n\t\t\t\twrong_answer(\"Expected float, got: %s\", team.c_str());\n\t\t\t}\n\t\t\tif (!equal(tval, jval, float_abs_tol, float_rel_tol)) {\n\t\t\t\twrong_answer(\"Too large difference.\\n Judge: %s\\n Team: %s\\n Difference: %Lg\\n (abs tol %Lg rel tol %Lg)\",\n\t\t\t\t\t\t\t judge.c_str(), team.c_str(), fabsl(jval-tval), float_abs_tol, float_rel_tol);\n\t\t\t}\n\t\t} else if (case_sensitive) {\n\t\t\tif (strcmp(judge.c_str(), team.c_str()) != 0) {\n\t\t\t\twrong_answer(\"String tokens mismatch\\nJudge: `%s`\\nTeam: `%s`\", judge.c_str(), team.c_str());\n\t\t\t}\n\t\t} else {\n\t\t\tif(strcasecmp(judge.c_str(), team.c_str()) != 0) {\n\t\t\t\twrong_answer(\"String tokens mismatch\\nJudge: `%s`\\nTeam: `%s`\", judge.c_str(), team.c_str());\n\t\t\t}\n\t\t}\n\t\tjudgeans_pos += judge.length();\n\t\tstdin_pos += team.length();\n\t}\n\n\tif (new_stdin >> team) {\n\t\twrong_answer(\"Trailing output:\\n%s\", team.c_str());\n\t}\n\t\n\t// Close the files before copying\n\tif (judgemessage) fclose(judgemessage);\n\tif (diffpos) fclose(diffpos);\n\t\n\t// Copy the judgemessage.txt content to compare_result.txt\n\tcopyFileContent(\"judgemessage.txt\", \"compare_result.txt\");\n\t\n\texit(EXIT_AC);\n}",
			},
		},
	},
	{
		ID: "claude_3.7",
		Files: []models.File{
			{
				Name:    "main.py",
				Content: "#!/usr/bin/env python3\n\"\"\"\nCompare Script for Code Output Grading\n\nThis script compares the solution output with submitted output and checks for:\n1. Case differences\n2. Spacing differences\n3. Overall identity\n\nResults are stored in compare_result.txt. If outputs are identical, an empty file is created.\nIf there are differences, detailed information about the mismatches is written to the file.\n\"\"\"\n\nimport os\nimport sys\nimport re\n\n\ndef compare_outputs(solution_file, submission_file):\n    \"\"\"\n    Compare the contents of solution file and submission file.\n    \n    Args:\n        solution_file (str): Path to the solution output file\n        submission_file (str): Path to the submitted output file\n        \n    Returns:\n        tuple: (is_identical, differences_message)\n    \"\"\"\n    # Read the files\n    try:\n        with open(solution_file, 'r', encoding='utf-8') as f:\n            solution_content = f.read()\n    except FileNotFoundError:\n        return False, f\"Error: Solution file '{solution_file}' not found.\"\n    except Exception as e:\n        return False, f\"Error reading solution file: {str(e)}\"\n    \n    try:\n        with open(submission_file, 'r', encoding='utf-8') as f:\n            submission_content = f.read()\n    except FileNotFoundError:\n        return False, f\"Error: Submission file '{submission_file}' not found.\"\n    except Exception as e:\n        return False, f\"Error reading submission file: {str(e)}\"\n    \n    # Check for exact match\n    if solution_content == submission_content:\n        return True, \"\"\n    \n    # Initialize differences message\n    differences = []\n    \n    # Split into lines for comparison\n    solution_lines = solution_content.splitlines()\n    submission_lines = submission_content.splitlines()\n    \n    # Check line count differences\n    if len(solution_lines) != len(submission_lines):\n        differences.append(f\"Line count mismatch: Solution has {len(solution_lines)} lines, Submission has {len(submission_lines)} lines.\")\n    \n    # Compare each line\n    for i in range(min(len(solution_lines), len(submission_lines))):\n        sol_line = solution_lines[i]\n        sub_line = submission_lines[i]\n        \n        if sol_line == sub_line:\n            continue\n        \n        # Check for case differences\n        if sol_line.lower() == sub_line.lower():\n            differences.append(f\"Line {i+1} has case differences:\")\n            differences.append(f\"  Solution: '{sol_line}'\")\n            differences.append(f\"  Submission: '{sub_line}'\")\n            continue\n        \n        # Check for whitespace differences\n        sol_stripped = re.sub(r'\\s+', ' ', sol_line).strip()\n        sub_stripped = re.sub(r'\\s+', ' ', sub_line).strip()\n        \n        if sol_stripped == sub_stripped:\n            differences.append(f\"Line {i+1} has whitespace differences:\")\n            differences.append(f\"  Solution: '{sol_line}'\")\n            differences.append(f\"  Submission: '{sub_line}'\")\n            continue\n        \n        # Content is different\n        differences.append(f\"Line {i+1} has content differences:\")\n        differences.append(f\"  Solution: '{sol_line}'\")\n        differences.append(f\"  Submission: '{sub_line}'\")\n        \n        # Find exact character position of difference\n        for j in range(min(len(sol_line), len(sub_line))):\n            if sol_line[j] != sub_line[j]:\n                differences.append(f\"  First difference at character {j+1}: '{sol_line[j]}' vs '{sub_line[j]}'\")\n                break\n    \n    # Check if solution has more lines\n    if len(solution_lines) > len(submission_lines):\n        differences.append(f\"Solution has {len(solution_lines) - len(submission_lines)} additional lines:\")\n        for i in range(len(submission_lines), len(solution_lines)):\n            differences.append(f\"  Line {i+1}: '{solution_lines[i]}'\")\n    \n    # Check if submission has more lines\n    if len(submission_lines) > len(solution_lines):\n        differences.append(f\"Submission has {len(submission_lines) - len(solution_lines)} additional lines:\")\n        for i in range(len(solution_lines), len(submission_lines)):\n            differences.append(f\"  Line {i+1}: '{submission_lines[i]}'\")\n    \n    return False, \"\\n\".join(differences)\n\n\ndef main():\n    \"\"\"Main function to handle command-line arguments and save results.\"\"\"\n    if len(sys.argv) != 3:\n        print(\"Usage: python compare.py <solution_file> <submission_file>\")\n        sys.exit(1)\n        \n    solution_file = sys.argv[1]\n    submission_file = sys.argv[2]\n    \n    is_identical, differences = compare_outputs(solution_file, submission_file)\n    \n    # Create the result file (empty if identical, with details if different)\n    with open('compare_result.txt', 'w', encoding='utf-8') as f:\n        if not is_identical:\n            f.write(differences)\n    \n    # Exit with appropriate code\n    if is_identical:\n        print(\"Files are identical. Empty compare_result.txt created.\")\n        sys.exit(0)\n    else:\n        print(\"Files differ. Details written to compare_result.txt\")\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()",
			},
		},
		RunScript: "#!/bin/bash\n\n$RUN_NAME sol_output output",
		RunName:   "main.py",
	},
}
